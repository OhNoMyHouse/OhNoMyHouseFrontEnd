<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.happyhouse.model.dao.AddressMapper">

    <select id="selectSido" resultType="AddressDto">
        SELECT DISTINCT sidoName
        FROM baseaddress
    </select>

    <select id="selectGugun" parameterType="string" resultType="AddressDto">
        SELECT DISTINCT gugunName
        FROM baseaddress
        WHERE sidoName = #{sidoName}
    </select>

    <select id="selectdong" parameterType="string" resultType="AddressDto">
        SELECT DISTINCT dongName
        FROM baseaddress
        WHERE sidoName = ${sidoName}
          AND gugunName = ${gugunName}
    </select>

    <select id="selectDongCode" parameterType="string" resultType="string">
        SELECT dongCode
        FROM baseaddress
        WHERE sidoName = ${sidoName}
          AND gugunName = ${gugunName}
          AND dongName = ${dongName}
    </select>

</mapper>